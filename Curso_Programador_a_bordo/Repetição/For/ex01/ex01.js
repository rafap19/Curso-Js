/* Não tem necessidade de se criar variáveis fora por exemplo,tudo está integrado dentro do for,seu valores tem uma ordem: Variável,condição e incremento */

for(var contador = 0; contador < 10; contador ++) {
    console.log(contador);
}

