function soma(a,b) {
    return a + b;
}
var resultadoSoma = soma(10,20);
    console.log(resultadoSoma)

    /* Podemos criar uma função chamada soma e dar dois atributos para a mesma,sendo no caso a e b.

       Depois disso usamos o comando return e declaramos o valor que queremos criar para tais atributos,no caso somamos a e b.

       criamos uma variavel de nome resultadoSoma e chamamos a função soma,e por fim declaramos os valores de a e b.
    */ 