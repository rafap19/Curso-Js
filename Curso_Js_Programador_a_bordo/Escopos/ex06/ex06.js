/* não podemos criar duas variaveis de mesmo nome com o let,o var já permite isso */

let nome = 'Rafael';
    console.log('nome')
let nome = 'Sousa'/*se criarmos com var ela funciona,com o let não,para que o erro seja corrigido precisamos criar o segundo let em um bloco isolado, ex:

{
    let nome = 'Sousa'
} */

   