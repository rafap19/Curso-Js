           /*propriedade join

    1 - console.log(variavel.join); = Junta os valores do array e transforma em uma string,para isso precisamos no mínimo usar um separador vazio '',mas também podemos escolher algo para separá-los*/

    var numeros = [10,12,14,16,18,20];
           console.log(numeros.join('---'));